{
    "name": "a2",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node ./index.js",
        "open": "start http://localhost:3000",

        "docker-build": "docker-compose build",
        "docker-up": "docker-compose up",
        "docker-down": "docker-compose up",
        "docker-ps": "docker ps",
        "docker-stats": "docker stats",
        "docker-rm": "FOR /f \"tokens=*\" %i IN ('docker ps -a -q') DO docker rm %i",
        "docker-rmi": "docker rmi $(docker images -q -f \"dangling=true\")",
        "docker-prune": "docker system prune"

    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "express": "^4.14.0",
        "body-parser": "^1.12.0",
        "mongodb": "^2.0.25",
        "morgan": "^1.5.1"
    },
    "devDependencies": {
        "debug": "^2.1.2",
        "mocha": "^2.1.0",
        "nodemon": "^1.3.7",
        "supertest": "^0.15.0"
    }
}